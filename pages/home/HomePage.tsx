import MainBanner from "./ui/MainBanner";
const HomePage = () => {
  // 추후 컨텐츠가 추가될 수 있기때문에 컴포넌트 분리
  return (
    <section className="">
      <MainBanner />
    </section>
  );
};

export default HomePage;
